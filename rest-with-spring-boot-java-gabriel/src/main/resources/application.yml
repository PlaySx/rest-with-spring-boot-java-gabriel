spring:
  application:
    name: rest-with-spring-boot-java-gabriel
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_with_spring_boot_java_gabriel?userTimezone=true&serverTimezone=UTC
    username: root
    password: Gabriel2003@
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect:
        show-sql: false
    open-in-view: false

logging:
  level:
    root: WARN
    br.com.gabriel: DEBUG